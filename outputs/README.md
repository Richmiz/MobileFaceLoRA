# Outputs

Only lightweight figures are tracked here:

- training_curves.png: training loss and accuracy from the latest saved run.
- training_samples.png: a visualization of augmented student-side inputs.

Model checkpoints and deployment exports are intentionally excluded. See docs/ARTIFACTS.md for the artifact policy and the recorded local checkpoint identity.
